---
import Layout from '../layouts/Layout.astro';

---

<Layout title="Bettools - Login" description="">
	<main>
		<div class="flex items-center justify-center min-h-screen bg-[radial-gradient(circle_at_bottom_left,_var(--tw-gradient-stops))] from-lime-200 via-cyan-400 to-green-200">
			<div class="card p-8 w-96 bg-white"> <!-- Usando a classe card para criar um elemento com borda e sombra -->
				<h1 class="text-3xl font-bold text-center text-blue-600 text-transparent bg-clip-text bg-gradient-to-r from-lime-400 to-cyan-500">Bettools</h1>
				<p class="text-gray-500 text-center mb-4">Entre com sua conta para acessar o melhor conteúdo esportivo.</p>
				<form>
					<div class="mb-4">
						<label for="email" class="block text-gray-700 text-sm font-medium">Email</label>
						<input type="email" id="email" name="email" class="input input-bordered w-full" placeholder="Digite seu email"> <!-- Usando a classe input para criar um campo de entrada com borda e foco -->
					</div>
					<div class="mb-4">
						<label for="password" class="block text-gray-700 text-sm font-medium">Senha</label>
						<input type="password" id="password" name="password" class="input input-bordered w-full" placeholder="Digite sua senha">
					</div>
					<div class="mb-4 flex items-center justify-between">
						<div class="flex items-center">
							<input type="checkbox" id="remember" name="remember" class="checkbox checkbox-accent"> <!-- Usando a classe checkbox para criar uma caixa de seleção com cor de destaque -->
							<label for="remember" class="ml-2 text-gray-700 text-sm">Lembrar de mim</label>
						</div>
						<a href="#" class="text-blue-600 text-sm hover:underline">Esqueceu sua senha?</a>
					</div>
					<div class="mb-4">
						<button type="submit" class="btn btn-primary w-full">Entrar</button> <!-- Usando a classe btn para criar um botão com cor primária -->
					</div>
				</form>
				<p class="text-gray-500 text-center">Não tem uma conta? <a href="#" class="text-blue-600 hover:underline">Cadastre-se</a></p>
			</div>
		</div>
	</main>
</Layout>